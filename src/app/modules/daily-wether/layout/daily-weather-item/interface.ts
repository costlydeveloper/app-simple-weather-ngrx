export interface DisplayWeatherItem {
  dayName: string;
  iconURL: string;
  tempMax: string;
  tempMin: string;
}

export interface OneDayWeather {
  hour: string;
  iconURL: string;
  temp: string;
}
